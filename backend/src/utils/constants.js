import 'dotenv/config'

export const {
    SERVER_PORT,
    SERVER_HOST,
    DATABASE_URL,
    DB_USER,
    DB_PASSWORD,
    DB_HOST,
    DB_PORT,
    DB_NAME,
    EMAIL_NAME,
    EMAIL_USER,
    EMAIL_PASS,
    BASE_URL,
    RECEIPIENT_EMAIL,
    CLOUDINARY_API_KEY,
    CLOUDINARY_API_SECRET,
    CLOUDINARY_CLOUD_NAME
} = process.env
